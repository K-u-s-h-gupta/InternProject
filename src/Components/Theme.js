import { createTheme } from "@mui/material";

const theme = createTheme({
  typography: {
    fontFamily: "Poppins",
  },
  tablecell: {
    fontFamily: "Poppins",
  },
  textbox: {
    fontFamily: "Poppins",
  },
});

export default theme;
